<!-- 
	git init
	git add
	git status
	git commit
	git diff : xem nhung noi dung thay doi modify

	working directory
	staging area
	git repository

	git checkout --file: --xoa noi dung vua thay doi chua commit
	git reset: tu repository xuog stagin area

	git checkout -b <branch> (brancking)
	git checkout branch: tao nhanh
	git merge :ghep nhanh
	git checkout -d <branch>

	git reset --soft <to_commit>
	git reset --mixed 
	git reset --hard

	git revrt <commit>: quay lai trang thai luc truoc
	gitignore: bo qua 1 so file ma ko muon commit

	github
	git remote add origin url

	git config --global credential.helper  "cache --timeout=1800"
 -->